# Example Environment Variables
# Copy this file to env.production or env.development and customize the values

# API Configuration
# Base URL for the backend API
API_BASE_URL=http://localhost:8081

# API request timeout in milliseconds
API_TIMEOUT=30000

# Feature Flags
# Enable/disable mock data (useful for development)
ENABLE_MOCK_DATA=false

# Application Settings
NODE_ENV=production
VITE_APP_TITLE=Release Tracker
VITE_APP_VERSION=1.0.0

# Logging Level (debug, info, warn, error)
LOG_LEVEL=info

# Optional: Database Configuration (if needed)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=release_tracker
# DB_USER=postgres
# DB_PASSWORD=password

# Optional: Authentication Configuration (if needed)
# AUTH_ENABLED=true
# AUTH_PROVIDER=keycloak
# AUTH_URL=https://auth.example.com
# AUTH_CLIENT_ID=release-tracker
# AUTH_CLIENT_SECRET=your-secret-here 